extends layout

block main
  .col-lg-12
    .not-found.clearfix
      h3 #{code} 
        - if (msg)
          span.msg #{msg}
        - else 
          span 啊哦，服务器或者数据库出问题了，麻烦联络管理员解决。
        - if (error)
          p.error-detail #{error.toString()}